# PoE 시세 오버레이 — 요구사항 명세

| | |
|---|---|
| 문서 상태 | 확정 (1차 범위) |
| 작성일 | 2026-08-15 |
| 대상 게임 | Path of Exile 1 |
| 사용자 | 본인 전용 |

---

## 1. 목적

PoE 플레이 중 관심 있는 통화성 아이템(갑충석·정수·생기 등)의 시세를, 게임을 벗어나지 않고 화면 위에서 상시 확인한다.

---

## 2. 범위

### 1차 범위에 포함

- 고정 관심목록에 담은 통화성 아이템의 현재 시세 상시 표시
- 주기적 자동 갱신
- 게임 화면 위 오버레이 (클릭 통과)
- 리그 전환, 갱신 주기 설정

### 1차 범위에서 제외

| 제외 항목 | 사유 |
|---|---|
| 이력 차트 / 시계열 축적 | API가 시계열을 주지 않음. 변동률은 API가 계산해서 제공 |
| 가격 알림 | 먼저 "보이는 것"을 완성한 뒤 실사용 후 판단 |
| 고유 아이템·스킬 젬·지도·착용 장비 | 모델이 복잡해지고 다른 API 형태가 필요 |
| 공식 Trade API 연동 | OAuth·레이트리밋·약관 제약. 스택 아이템엔 poe.ninja로 충분 |
| 한글 이름 표시 | **구조(i18n)는 1일차부터 넣되 사전은 나중에 채움** |
| 전역 핫키, 게임 프로세스 감지, 트레이 아이콘 | 부분 클릭 통과로 조작 문제가 해결됨 |
| 배포 패키징·자동 업데이트 | 본인 전용 로컬 빌드 |

---

## 3. 용어

| 한글 | 영문 | poe.ninja 위치 |
|---|---|---|
| 갑충석 | Scarab | `Scarab` 카테고리 (115종) |
| 정수 | Essence | `Essence` 카테고리 (101종) |
| 생기 | Crystallised Lifeforce | **`Currency` 카테고리 안에 4종** |
| 혈기생기 | Vivid Crystallised Lifeforce | `vivid-lifeforce` |

> ⚠️ poe.ninja가 주는 카테고리 한글 라벨은 한국어 클라이언트 용어와 다르다.
> 예: Scarab → poe.ninja `"쇠똥구리"` vs 게임 내 `"갑충석"`. **기성 라벨을 그대로 쓰면 안 된다.**

---

## 4. 기능 요구사항

### FR-01 관심목록

- FR-01-1 사용자가 아이템을 **개별로 검색해 관심목록에 추가**한다.
- FR-01-2 관심목록에서 아이템을 제거한다.
- FR-01-3 개수 제한 없음. 화면을 넘치면 스크롤한다.
- FR-01-4 관심목록은 설정 파일에 영속 저장되어 재시작 시 복원된다.
- FR-01-5 아이템 식별은 poe.ninja `id` 슬러그(`vivid-lifeforce`)를 키로 한다. 표시 이름이 아니다.

### FR-02 시세 조회

- FR-02-1 데이터 소스는 **poe.ninja 단독**.
- FR-02-2 조회 대상은 통화성 소모품 카테고리(exchange)에 한한다.
- FR-02-3 리그를 지정하지 않으면 현재 챌린지 리그를 사용한다.
- FR-02-4 리그 목록은 **런타임에 조회**한다. 리그명은 3개월마다 바뀌므로 상수로 두지 않는다.
- FR-02-5 폴링 시 **Divine Orb 시세(`Currency` / `id: "divine"`)를 항상 함께 조회**한다. 디바인 환산에 필요하다.

### FR-03 자동 갱신

- FR-03-1 기본 **5분** 주기로 폴링한다. 설정에서 변경 가능.
- FR-03-2 관심목록에 담긴 아이템의 카테고리 단위로 묶어 호출한다. 아이템 하나당 한 번씩 호출하지 않는다.
- FR-03-3 실패 시 재시도한다(지수 백오프). 마지막 성공 값을 계속 표시한다.
- FR-03-4 값이 얼마나 오래됐는지는 `마지막 갱신 시각` 표시로 사용자가 판단한다.

### FR-04 표시

각 줄에 다음을 보여준다.

| 항목 | 형식 |
|---|---|
| 아이템 이름 | 영문 (i18n 계층 경유) |
| 현재가 | 아래 규칙 |
| 변동률 | `▲2.9%` / `▼6.2%`, 상승·하락 색상 구분 |
| 마지막 갱신 시각 | `2분 전` (화면 하단에 한 번) |

- FR-04-1 거래량(volume)은 표시하지 않는다.
- FR-04-2 저거래량 노이즈에 대한 별도 처리를 하지 않는다. 관심목록은 사용자가 직접 고르므로 감수한다.

#### FR-04-3 표시 통화 선택

아이템마다 실제로 거래되는 기준 통화가 다르다. 생기는 주로 디바인으로, 갑충석은 카오스로 거래된다.

- **아이템별로 `자동 / 카오스 / 디바인`을 선택**할 수 있다.
- 기본값은 **`자동`** 이며, 응답의 **`topPair.currency`를 따른다.** 이 필드가 곧 해당 아이템이 실제로 가장 많이 거래되는 상대 통화다.
  - 실측: 생기 3종 → `divine` · 갑충석 → `chaos` · 정수는 항목별로 섞임
- 전역 기본값도 설정에서 지정할 수 있다 (`자동` / `카오스` / `디바인`).

#### FR-04-4 현재가 표기 형식

선택된 통화 기준으로, 단가가 1 미만이면 역수로 표기한다. 생기처럼 개당 0.06카오스인 아이템은 그대로 띄우면 읽을 수 없기 때문이다.

| 선택 통화 | 조건 | 표기 | 예 |
|---|---|---|---|
| 카오스 | 1c 이상 & 1d 이상 | `359.7c (1.85d)` 병기 | 뿔 갑충석 |
| 카오스 | 1c 이상 | `43.5c` | Sacred 생기 |
| 카오스 | 1c 미만 | `1c당 15.5개` | 혈기생기 |
| 디바인 | 1d 이상 | `1.85d` | 뿔 갑충석 |
| 디바인 | 1d 미만 | `1d당 3,026개` | 혈기생기 |

#### FR-04-5 환산 정밀도

- 디바인 환산은 **`value.amount ÷ Divine Orb 시세`로 직접 계산**한다.
- **응답의 `valueAlt`를 역수 계산에 쓰지 않는다.** 소수 4자리로 반올림되어 있어 저가 아이템에서 오차가 10%까지 벌어진다.
  - 실측: 혈기생기 `valueAlt = 0.0003` → 역수 `3,333개` ❌
  - 직접 계산 `0.0644 ÷ 194.9` → `3,026개` ✅ (`topPair.rate`와 일치)
- Divine Orb 시세는 `Currency` 카테고리의 `id: "divine"`에서 얻는다.

### FR-05 오버레이 동작

- FR-05-1 항상 최상위(`Topmost`)로 표시된다.
- FR-05-2 **부분 클릭 통과**: 목록 본문 영역은 마우스가 게임으로 통과하고, 상단바·드래그 그립 영역에서만 클릭이 받아진다.
  - 커서 위치를 감지해 `WS_EX_TRANSPARENT`를 동적으로 여닫는 방식으로 구현한다.
- FR-05-3 창을 클릭해도 게임이 최소화되지 않는다 (`WS_EX_NOACTIVATE`).
- FR-05-4 창 위치·크기를 저장하고 재시작 시 복원한다. 저장된 위치가 현재 모니터 구성에서 화면 밖이면 기본 위치로 되돌린다.
- FR-05-5 투명도를 조절할 수 있다. (기존 구현 유지)

### FR-06 설정

리그 · 갱신 주기 · 창 위치/크기 · 투명도 · 관심목록 · 표시 언어 · **표시 통화(전역 기본값 및 아이템별 설정)** 를 저장한다.

### FR-07 i18n

- FR-07-1 **모든 표시 문자열은 ID로 조회**한다. 코드에 리터럴을 직접 쓰지 않는다.
- FR-07-2 아이템 이름의 번역 키는 poe.ninja `id` 슬러그다.
- FR-07-3 1차 릴리스는 영문 사전만 채운다. 한글 사전은 이후 추가하며, **이때 코드 변경이 필요해서는 안 된다.**
- FR-07-4 UI 문구(버튼·라벨)도 동일한 방식으로 키를 갖는다.

---

## 5. 비기능 요구사항

- NFR-01 게임과 동시 실행되므로 유휴 시 CPU 점유가 무시할 수준이어야 한다.
- NFR-02 poe.ninja에 과도한 요청을 보내지 않는다. 카테고리 단위 호출 + 최소 5분 주기.
- NFR-03 네트워크 실패가 앱을 종료시키지 않는다.
- NFR-04 게임은 **테두리 없는 창(Windowed Fullscreen)** 모드로 실행하는 것을 전제한다. 전용 전체화면에서는 어떤 오버레이도 표시되지 않으며, DirectX 훅 인젝션은 안티치트 위험 때문에 사용하지 않는다.
- NFR-05 poe.ninja는 GGG와 무관한 커뮤니티 사이트임을 앱 내에 출처로 밝힌다.

---

## 6. 데이터 소스 명세 (실측 확인)

**엔드포인트**

```
https://poe.ninja/poe1/api/economy/exchange/current/overview?league={league}&type={category}
```

**응답 항목별 필드**

| 필드 | 예 | 용도 |
|---|---|---|
| `id` | `vivid-lifeforce` | **안정적 식별자 & 번역 키** |
| `name` | `Vivid Crystallised Lifeforce` | 영문 표시명 |
| `value.amount` / `.currency` | `0.0644` / `chaos` | 현재가 |
| `valueAlt` | `0.0003 divine` | 디바인 환산. **소수 4자리 반올림 — 역수 계산에 쓰지 말 것** |
| `volume` | `2050562` | 신뢰도 지표 (1차 미사용) |
| `topPair` | `{divine, 3026}` | **주 거래 통화 판별** + 대량 거래 비율 |
| `changePercent` | `26.12` | **변동률. 직접 계산 불필요** |

**exchange 카테고리 18종**

`Currency` · `Fragment` · `Runegraft` · `AllflameEmber` · `Tattoo` · `Omen` · `DjinnCoin` · `Ducat` · `EnshroudingCrystal` · `DivinationCard` · `Artifact` · `Oil` · `DeliriumOrb` · `Scarab` · `Astrolabe` · `Fossil` · `Resonator` · `Essence`

**주의**

1. 응답에 **시계열 이력이 없다.** 현재 스냅샷만 제공된다.
2. `locale`을 지정해도 **아이템 `name`은 영문으로만** 온다.
3. 기준 통화는 카오스다.
4. `volume`이 낮은 항목의 `changePercent`는 크게 튄다 (실측: volume 9.67 → `+65.71%`).
5. **`valueAlt`는 소수 4자리로 반올림된다.** 저가 아이템에서 역수를 취하면 오차가 10%까지 벌어지므로,
   디바인 환산은 반드시 `value.amount ÷ Divine Orb 시세`로 직접 계산한다. → FR-04-5
6. `topPair.currency`는 해당 아이템이 실제로 가장 많이 거래되는 상대 통화를 알려준다.
   표시 통화 `자동` 모드의 판단 근거로 쓴다.

---

## 7. 기술 스택

| 구분 | 선택 | 비고 |
|---|---|---|
| 런타임 | .NET 8 (`net8.0-windows`) | **SDK 설치 필요** — 현재 런타임만 있음 |
| UI | WPF | Win32 상호운용이 자유로워 오버레이에 최적 |
| MVVM | `CommunityToolkit.Mvvm` | 수동 `INotifyPropertyChanged` 제거 |
| 호스팅/DI/설정 | `Microsoft.Extensions.Hosting` | 폴링은 `BackgroundService` + `PeriodicTimer` |
| HTTP 복원력 | `Microsoft.Extensions.Http` + `Polly` | 재시도·타임아웃·서킷브레이커 |
| 차트 | `OxyPlot.Wpf` | **1차 미사용**, 이력 차트 도입 시 사용 |
| 제거 | ~~`AngleSharp`~~ | 전부 JSON이라 HTML 파싱이 불필요 |
| 보류 | ~~`Microsoft.Data.Sqlite`~~ | 시계열을 안 쌓으므로 불필요. 이력 차트 도입 시 재검토 |
| 테스트 | xUnit — 핵심 로직만 | 응답 파싱·통화 환산·설정 직렬화 |

---

## 8. 설정 파일

위치: `%APPDATA%\PoeOverlay\settings.json`

```jsonc
{
  "league": null,              // null = 현재 챌린지 리그 자동
  "refreshIntervalMinutes": 5,
  "language": "en",            // "ko" 사전 추가 시 전환
  "defaultDisplayCurrency": "auto",   // "auto" | "chaos" | "divine"
  "window": { "x": 100, "y": 100, "width": 420, "height": 500, "opacity": 0.87 },
  "watchlist": [
    // displayCurrency 생략 시 defaultDisplayCurrency 를 따름
    { "id": "vivid-lifeforce",   "category": "Currency", "displayCurrency": "divine" },
    { "id": "essence-of-horror", "category": "Essence" },
    { "id": "horned-scarab-of-pandemonium", "category": "Scarab", "displayCurrency": "chaos" }
  ]
}
```

---

## 9. 결정 이력

| ID | 결정 | 근거 |
|---|---|---|
| A1 | PoE1 | 갑충석·생기 모두 PoE1 |
| A2 | 현재 리그 기본, 설정에서 전환 | 드롭다운 하나로 비용이 작음 |
| A3 | 본인 전용 | 설치 플로·자동 업데이트 불필요 |
| A4 | 상시 감시형 | "실시간에 준하는 주기로 확인" 요구에 부합 |
| B1 | 개별로 골라 담기 | 갑충석만 115종이라 통째 표시는 노이즈 |
| B2 | 개수 제한 없음, 스크롤 | 선별 방식이라 자연히 소규모 |
| B3 | 통화성 소모품만 | 모델이 단순하고 단일 API로 해결 |
| B4 | 프리셋 제외 | 본인 전용, 목록 하나로 충분 |
| B5 | 생기 = Lifeforce, `Currency` 카테고리 | 실측 확인 |
| C1 | poe.ninja 단독 | 인증 불필요, 스택 아이템엔 충분 |
| C2 | 5분 기본 + 설정 | 집계 스냅샷이라 더 짧게 해도 무의미 |
| C3 | 이력 미축적 | API가 시계열 미제공, `changePercent`로 대체 |
| C4 | 해당 없음 | 시계열을 안 쌓음 |
| C5 | 마지막 성공값 유지 + 재시도 | `마지막 갱신 시각` 표시로 노후 판단 |
| D2 | 차트 없음 | 상시 감시엔 현재가+변동률로 충분 |
| D3 | 현재가·변동률·갱신시각 | 오버레이 가로 폭 한정 |
| D4 | 노이즈 필터 없음 | 직접 고른 목록이라 감수 |
| D5 | 표시는 영문, **i18n 구조는 1일차부터** | 나중에 사전만 얹으면 전환 |
| D6 | 단가 1 미만은 역수 표기 | 생기가 0.0644c — 실측에서 발견 |
| D7 | 표시 통화를 아이템별로 선택, 기본 `자동` | 생기는 디바인, 갑충석은 카오스로 거래됨. `topPair.currency`가 이를 알려줌 |
| D8 | 디바인 환산은 직접 계산 | `valueAlt`가 4자리 반올림이라 저가 아이템에서 10% 오차 |
| E1 | 전역 핫키 제외 | 부분 클릭 통과로 불필요해짐 |
| E2 | 부분 클릭 통과 | 전체 통과 시 조작 수단이 사라지는 모순 해소 |
| E3 | 창 위치·크기 기억 | 체감 대비 구현이 간단 |
| E4/E5 | 트레이·게임 감지 제외 | 1차 불필요 |
| F1 | 알림 제외 | 실사용 후 판단 |
| G1 | 로컬 빌드 | 본인 전용 |
| G2 | `%APPDATA%\PoeOverlay\settings.json` | — |
| G3 | 핵심 로직만 단위 테스트 | API 응답 형식 변화 감지가 주 목적 |

---

## 10. 착수 전 선결 과제

1. **.NET 8 SDK 설치** — 현재 런타임 8.0.30만 있어 빌드 불가.
2. **PR #2 머지** — `main`보다 구조가 낫고, 머지 전엔 그 위에 쌓는 작업이 전부 충돌한다.

---

## 11. 추후 검토

- 이력 차트 (poe.ninja 과거 데이터 엔드포인트 존재 여부 조사 → 없으면 직접 축적 + SQLite)
- 한글 사전 확보 (게임 클라이언트 용어 기준. poe.ninja 라벨은 사용 불가)
- 목표가 / 급변동 알림
- 저거래량 신뢰도 표식
- 고유 아이템·젬·지도 확장
